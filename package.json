{
  "name": "tailwind-src",
  "version": "15.0.0",
  "description": "tailwind userscript bundle, un-bundled. just for reading, not a build. also hosts the private chat relay.",
  "author": "jason",
  "license": "MIT",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
